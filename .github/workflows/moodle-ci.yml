name: Moodle Plugin CI

########################################################################################################################

on: [ push, pull_request ]

#######################################################################################################################

jobs:
  call-moodle-ci-workflow:
    uses: Opencast-Moodle/moodle-workflows-opencast/.github/workflows/moodle-ci.yml@main
    with:
      requires-tool-plugin: true
      requires-block-plugin: true
      requires-hvp-plugin: true
      branch-tool-plugin: jwt-405 # For testing only in this branch
      branch-block-plugin: jwt-405 # For testing only in this branch
      branch-hvp-plugin: stable
      branch-opencast-docker-default: 17.1
